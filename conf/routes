# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET        /                    controllers.Application.index
GET        /player/:name        controllers.ComputerCraft.player(name: String)
GET        /:id/:index          controllers.ComputerCraft.get(id: String, index: String)


